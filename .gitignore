# Uploaded files and privacy data
uploads/*
!uploads/.gitkeep
storage/*
!storage/.gitkeep
images/uploads/*
!images/uploads/.gitkeep

# Local DB/file backups (contain student personal data — never push)
backups/

# Full DB dump with real user logins/emails/password hashes — never push
/digital_research.sql

# Environment variables and config containing passwords
.env
.env.local
.env.*
config/database.php
config/config.php

# Agent Scratch Files
scratch/
scratch.php
scratch_uploads.php

# Claude Code local settings
.claude/

# Dependencies
vendor/
node_modules/

# OS Generated Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
